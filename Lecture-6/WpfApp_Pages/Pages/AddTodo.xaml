<Page x:Class="WpfApp_Pages.Pages.AddTodo"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp_Pages.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Add Todo">

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Add Todo" FontSize="22" FontWeight="Medium" Margin="0 0 0 50" />

        <StackPanel Grid.Row="2">
            <TextBlock Text="Enter a activity..." />
            <TextBox x:Name="tb_Todo" Padding="5" Margin="0 5 0 25" />
            <Button x:Name="btn_Add" Content="ADD TODO" Padding="8" Click="btn_Add_Click" />
        </StackPanel>
    </Grid>
</Page>
